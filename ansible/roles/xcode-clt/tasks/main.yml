---
- name: Check if Xcode Command Line Tools are installed
  ansible.builtin.raw: xcode-select --print-path
  register: clt_check
  changed_when: false
  failed_when: false

- name: Install Xcode Command Line Tools
  ansible.builtin.script: install-clt.bash
  when: clt_check.rc != 0
  become: true
